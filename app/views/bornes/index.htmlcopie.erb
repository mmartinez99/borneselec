<p id="notice"><%= notice %></p>

<h1>bornes</h1>

<table>
  <thead>
    <tr>
      <th>Designpizz</th>
      <th>Tarifpizz</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @bornes.each do |borne| %>
      <tr>
        <td><%= borne.Designpizz %></td>
        <td><%= borne.Tarifpizz %></td>
        <td><%= link_to 'Show', borne %></td>
        <td><%= link_to 'Edit', edit_borne_path(borne) %></td>
        <td><%= link_to 'Destroy', borne, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New borne', new_borne_path %>
